function init(self)
	local message = {}
	message.collisions = { hash("cave"),hash("bomb") }
	message.debug = true
	message.deleteNodeAfterGotten = false
	msg.post("/NavGO_HandlerGO#NavGO_HandlerScript", hash("init"), message)
end

function on_message(self, message_id, message, sender)
end